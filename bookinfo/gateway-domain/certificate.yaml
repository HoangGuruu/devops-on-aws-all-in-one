apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: public-sites-cert
  namespace: default
spec:
  secretName: public-sites-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
  - bookinfo.hoangguruu.site
  # - grafana.hoangguruu.site