[defaults]
inventory = inventory.ini
host_key_checking = False
remote_user = ubuntu
private_key_file = ~/.ssh/k8s-cluster-key.pem
timeout = 30

[privilege_escalation]
become = True
become_method = sudo
become_user = root
